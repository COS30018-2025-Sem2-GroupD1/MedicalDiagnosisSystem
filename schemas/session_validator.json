{
	"$jsonSchema": {
		"bsonType": "object",
		"title": "Session validator",
		"required": [
			"account_id",
			"patient_id",
			"title",
			"created_at",
			"updated_at"
		],
		"properties": {
			"account_id": {
				"bsonType": "objectId",
				"description": "'account_id' must be an objectId and is required."
			},
			"patient_id": {
				"bsonType": "objectId",
				"description": "'patient_id' must be an objectId and is required."
			},
			"title": {
				"bsonType": "string",
				"description": "'title' must be an string and is optional."
			},
			"created_at": {
				"bsonType": "date",
				"description": "'created_at' must be a date and is required."
			},
			"updated_at": {
				"bsonType": "date",
				"description": "'updated_at' must be a date and is required."
			},
			"messages": {
				"bsonType": "array",
				"items": {
					"bsonType": "object",
					"title": "Message validator",
					"required": [
						"_id",
						"sent_by_user",
						"content",
						"timestamp"
					],
					"properties": {
						"_id": {
							"bsonType": "int",
							"minimum": 0,
							"description": "'_id' must be a int is required."
						},
						"sent_by_user": {
							"bsonType": "bool",
							"description": "'sent_by_user' must be a bool is required."
						},
						"content": {
							"bsonType": "string",
							"description": "'content' must be a string is required."
						},
						"timestamp": {
							"bsonType": "date",
							"description": "'timestamp' must be a date and is required."
						}
					}
				}
			}
		}
	}
}
