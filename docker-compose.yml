services:
   backend:
      build:
         context: .
         dockerfile: dockerfile
      ports:
         - "7860:7860"
      volumes:
         - ./app:/home/user/medical_diagnosis_system/app
      env_file:
         - ./.env
      restart: unless-stopped
